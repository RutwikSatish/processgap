name: Keep Alive

on:
  schedule:
    - cron: '0 */4 * * *'  # Runs every 4 hours

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s https://processgap.streamlit.app > /dev/null
